{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set for the main window. Frontend only uses invoke and event listening, so only core permissions are needed.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
